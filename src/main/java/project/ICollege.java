package project;

public interface ICollege {
    //that loads the college information from a given file location
    void loadCollegeInfo(String fileLocation);
    String listCompusesForCollage(String name);
}
